# Server
PORT=3000
NODE_ENV=development
INSTANCE_NAME=shiftmate-api-1
TRUST_PROXY_HOPS=0
SHUTDOWN_TIMEOUT_MS=10000
CORS_ALLOWED_ORIGINS=https://shift-calendar.co.kr,http://localhost:3000
REQUEST_BODY_LIMIT=100kb
AUTH_RATE_LIMIT_WINDOW_MS=60000
AUTH_RATE_LIMIT_MAX=10

# Work shift monthly cache (enable only after DB migration and Redis provisioning)
WORK_SHIFT_CACHE_ENABLED=false
REDIS_URL=redis://:replace-with-redis-password@shiftmate_cache:6379
REDIS_PASSWORD=replace-with-redis-password
CACHE_KEY_PREFIX=shiftmate:development
WORK_SHIFT_CACHE_TTL_SECONDS=86400
WORK_SHIFT_CACHE_TTL_JITTER_SECONDS=3600
WORK_SHIFT_CACHE_LOCK_MS=5000
WORK_SHIFT_CACHE_WAIT_MS=500
REDIS_CONNECT_TIMEOUT_MS=500
REDIS_COMMAND_TIMEOUT_MS=100
CACHE_OUTBOX_POLL_MS=1000
CACHE_OUTBOX_BATCH_SIZE=100

# Push notification outbox/worker (migration과 Firebase secret 준비 후 순차 활성화)
PUSH_JOB_ENQUEUE_ENABLED=false
PUSH_WORKER_ENABLED=false
PUSH_APP_ENVIRONMENT=STAGE
FIREBASE_PROJECT_ID=replace-with-stage-or-production-firebase-project-id
GOOGLE_APPLICATION_CREDENTIALS=/run/secrets/firebase.json
FIREBASE_SERVICE_ACCOUNT_FILE=/absolute/path/to/firebase-service-account.json
PUSH_JOB_POLL_MS=1000
PUSH_JOB_BATCH_SIZE=20
PUSH_JOB_LEASE_SECONDS=120
PUSH_MAX_ATTEMPTS=6
PUSH_JOB_TTL_SECONDS=3600
PUSH_TERMINAL_RETENTION_DAYS=30

# Group feature
GROUP_MEMBER_LIMIT=20
GROUP_INVITATION_TTL_DAYS=7
GROUP_CALENDAR_MAX_RANGE_DAYS=100
# Local/Stage에서만 명시적으로 true. Center 기본값은 false.
API_DOCS_ENABLED=false

# Database (PostgreSQL 16 - 홈서버 Docker에서는 DB 컨테이너의 Compose 서비스명 사용)
DB_HOST=replace-with-postgres-host-or-compose-service
DB_PORT=5432
DB_NAME=shift_calendar
DB_USER=postgres
DB_PASSWORD=replace-with-database-password
DB_SSL=false
DB_POOL_MAX=10
DB_POOL_MIN=0
DB_POOL_ACQUIRE_MS=30000
DB_POOL_IDLE_MS=10000

# JWT
JWT_SECRET=your-jwt-secret-key-here
JWT_REFRESH_SECRET=your-jwt-refresh-secret-key-here

# Kakao OAuth
KAKAO_CLIENT_ID=your-kakao-rest-api-key
KAKAO_CLIENT_SECRET=your-kakao-client-secret

# Naver OAuth
NAVER_CLIENT_ID=your-naver-client-id
NAVER_CLIENT_SECRET=your-naver-client-secret

# Apple OAuth (추후 사용)
# APPLE_CLIENT_ID=your-apple-client-id
# APPLE_TEAM_ID=your-apple-team-id
# APPLE_KEY_ID=your-apple-key-id
# APPLE_PRIVATE_KEY=your-apple-private-key
